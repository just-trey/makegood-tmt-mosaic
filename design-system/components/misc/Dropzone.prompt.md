A drag-and-drop target with a dashed border that lights up teal on drag-over — used for SVG artwork upload and STL/3MF part upload.

```jsx
<Dropzone label="Drop an SVG here, or click to browse" filename="smurfette.svg" onFiles={handleFiles} accept=".svg" />
```
