One row per detected SVG color: swatch, hex, area share, optional multi-select checkbox (for merging), and a per-color depth override.

```jsx
<ColorRow hex="#e8a13a" areaPct={34} depth={1.0} onDepthChange={setDepth} selected={false} onSelectedChange={setSelected} />
```
