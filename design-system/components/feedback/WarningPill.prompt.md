A soft-red inline warning banner, stacked over the viewport — used for recoverable geometry issues ("Boolean subtraction failed for color #e8a13a…").

```jsx
<WarningPill>Skipped a &lt;path&gt; with a gradient fill — element ignored.</WarningPill>
```
