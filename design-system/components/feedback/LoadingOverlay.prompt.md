A dimming overlay + spinner shown over the 3D viewport while geometry rebuilds. Place inside a `position: relative` container.

```jsx
<LoadingOverlay visible={isRebuilding} label="Rebuilding geometry…" />
```
