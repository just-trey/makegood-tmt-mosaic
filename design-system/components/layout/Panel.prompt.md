The repeating sidebar section: an uppercase, letter-spaced label followed by a hairline rule, then content. Every left-panel group in Mosaic (Artwork, Base part, Depth, Colors detected, Export…) uses this shell.

```jsx
<Panel title="Depth">
  <TextInput type="number" value={depth} onChange={setDepth} />
</Panel>
```
