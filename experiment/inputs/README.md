# Drop the reference files here

Copy these in from your local `stubs/`, keeping the names, then commit and push this branch.
`bootstrap.sh` reads this folder first and falls back to `stubs/`.

Verified slicer projects:

- `chair-body-all-parts.3mf`
- `chair-tower-reference-snapmaker.3mf`
- `chair-tower-reference-a1.3mf`
- `whlle-reference.3mf`
- `mosaic-wheel-snapmaker.3mf`
- `mosaic-wheel-mount-left.3mf`
- `dead-zones.3mf`
- the footrest reference project, whatever it is called (add a line to
  `experiment/bootstrap.sh`'s VERIFIED list, or just tell Claude the name)

Test designs:

- `mario.png`, `mario.webp`, `dino ring.svg`
- `temp/snoopy.svg`, `temp/Sunny MLP 2.svg`, `temp/pappa.svg`, `temp/smurfette.svg`

Photographs: one or more JPGs in `photos/`.
