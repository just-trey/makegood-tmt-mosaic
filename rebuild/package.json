{
  "name": "tmt-inlay",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --port 4174 --strictPort",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "evidence": "vite build && node scripts/evidence.mjs"
  },
  "dependencies": {
    "jszip": "^3.10.1",
    "manifold-3d": "^3.0.0",
    "polygon-clipping": "^0.15.7",
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/three": "^0.170.0",
    "playwright": "^1.50.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^3.0.0"
  }
}
